<!DOCTYPE HTML PUBLIC '-//W3C//DTD HTML 4.01 Transitional//EN'><html><head>
<title>PHP Dropbox App</title>
<meta http-equiv='Content-Type'
    content='text/html; charset=UTF-8'/>
    </head><body>
{{#users}}
<div style='border: 1px dashed gray; margin: 4px; padding: 5px; float: left'>
<h3 style='margin-top: 0px'>{{display_name}}</h3>
<ul style='padding: 0; list-style-type: none'>
{{#files}}
<li><a href='http://dl.dropbox.com/u/{{uid}}/{{contentPath}}/{{content}}'>
{{content}}
</a></li> {{/files}}
</ul>
</div> {{/users}}
{{#errorcode}}<div> <p style='clear: both'>
    {{{errorcode}}}
</p> </div>{{/errorcode}}

<h4 style='clear: both'>Missing something?</h4>
<pre style='margin-top: 0px'>hash("sha256",serialize($nightlies)) =
    {{hash}}</pre>
<div>
<p>Your name and files will only be displayed once you upload something.<br/>
Make sure your Dropbox's <code>Public/android-x86/</code> folder exists, and
you have posted at least one file.</p>
<p>We are up to precisely {{total_users}} users!
Only <em style='color: orange'>{{user_count}}</em> have posted anything, and
on this reading, none of them appeared to be nightly builds.  I wish that more
of you would post something.  I haven't built android myself in months and
tested it successfully, the mirror of those older builds is
<a href='http://martyfunkhouser.csh.rit.edu/~yebyen/android-x86/'>here</a>
(offline).</p>
</div>
<div>
<p>It was tested on an EeePC 2G Surf<br/>
If you are having trouble with this page (what kind of trouble?) try:</p>
<p><a href='oauth_destroysession.php'>Destroy Session</a></p>
<p>Otherwise, you could have a look at
<a href='./'>android-x86/</a> (the instructions).  Thanks!<br/>
<p>This page was rendered at: {{timestamp}}</p>
</div>
</body>
</html>
